import React from "react"

export default ({ children }) => (
  <div style={{ margin: `0 auto`, maxWidth: 650, padding: `0 1rem`, display: 'flex',  justifyContent:'center',  height: '90vh',  backgroundColor: 'lightblue' }}>
    {children}
  </div>
)
